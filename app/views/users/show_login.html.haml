-# show_login.html.haml using partial

%h1 Login

= form_tag auth_path, :method => :post do

  = label :user, :email, 'Email'
  = text_field :user, 'email'

  <br>
  <br>
  = label :user, :director, 'Password'
  = text_field :user, 'password'

  = submit_tag 'Log in'